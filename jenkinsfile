pipeline {
   agent any
   // triggers {
   //    pollSCM '* * * * *'
   // }
   stages{
      stage("hello") {
         steps{
            echo 'hello world'
         }
      }
      stage("build") {
         steps{
            echo 'building'
         }
      }
      stage("deploy") {
         steps{
            echo 'deploying'
         }
      }
      stage("release") {
         steps{
            echo 'releasing'
         }
      }
   }
}